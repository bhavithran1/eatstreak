# Claude Code: share the project's workflow, keep per-machine state out.
.claude/*
!.claude/skills/
!.claude/launch.json
.claude/settings.local.json

.DS_Store
node_modules/

# End-to-end scan suite output (regenerated every run).
tool/e2e/out/
